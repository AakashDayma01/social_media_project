from django.apps import AppConfig

class AccountsConfig(AppConfig):
    """
    Configuration class for the accounts application.

    This class handles the initialization and metadata configuration 
    for the 'apps.accounts' app within the Django project registry.
    """
    name = 'apps.accounts'
